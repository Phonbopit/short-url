{
  "name": "short-url",
  "version": "1.0.0",
  "dependencies": {
    "babel-preset-es2015": "^6.5.0",
    "chalk": "^1.1.1",
    "hapi": "^13.0.0",
    "mongoose": "^4.4.3",
    "vision": "^4.0.1"
  },
  "scripts": {
    "build": "./node_modules/.bin/babel src -d dist --copy-files",
    "dev": "npm run watch-js & npm run watch-server",
    "watch-js": "./node_modules/.bin/babel src -w -d dist --copy-files",
    "watch-server": "nodemon dist/index.js",
    "start": "node dist/index.js"
  }
}
